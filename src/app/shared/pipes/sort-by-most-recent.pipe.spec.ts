import { SortByMostRecentPipe } from './sort-by-most-recent.pipe';

describe('SortByMostRecentPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByMostRecentPipe();
    expect(pipe).toBeTruthy();
  });
});
